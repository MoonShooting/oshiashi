# 推し足(oshiashi) 프로젝트 문제점 분석 보고서

**작성일:** 2026-03-28
**대상 프로젝트:** 推し足 - 맞춤형 성지순례 및 공유 플랫폼
**팀:** 12기 2조 쿠노이치안주조작단 (김석현, 김지우, 안성민, 이상학, 조경철)
**분석 범위:** 프로젝트 제안서, 요구사항 정의서, ERD/DB 설계서, WBS, 프론트엔드 소스코드, 백엔드 소스코드

---

## 1. 프로젝트 개요

推し足(oshiashi)는 애니메이션, 드라마, 영화 등 콘텐츠의 실제 촬영지/배경 장소를 지도 기반으로 탐색하고, 맞춤형 성지순례 루트를 설계·공유할 수 있는 팬덤 특화 플랫폼이다. React 19 + Vite(프론트엔드), Spring Boot 3.x + Java 21(백엔드), MySQL(Aiven SaaS), Redis 환경에서 개발되었다.

---

## 2. 로딩 문제의 근본 원인 분석

### 2.1 프론트엔드 측 로딩 지연 요인

#### (1) 코드 스플리팅 전무 — 초기 번들 과대

`RouterList.jsx`에서 모든 페이지를 정적 import로 가져오고 있다. 15개 이상의 페이지 컴포넌트, 80개 이상의 하위 컴포넌트, 13개의 API 파일이 **최초 접속 시 한꺼번에 로드**된다. React.lazy()와 Suspense를 활용한 지연 로딩이 전혀 적용되지 않았다.

특히 `routePostApi.js`는 **단일 파일이 965줄**에 달하며, 게시글 정규화 로직, 루트 옵션 빌더, CRUD, 댓글, 북마크 처리까지 모두 포함하고 있어 트리셰이킹도 불가능하다.

#### (2) API 캐싱 부재 — 매번 재요청

React Query, SWR 등 데이터 페칭 라이브러리가 없다. 모든 API 호출이 native fetch를 통해 매번 새롭게 실행된다.

- 게시글 상세 진입 → 매번 post + comments 2건 요청
- 북마크 모달 오픈/닫기 반복 → 매번 전체 북마크 재요청
- 게시글 작성 페이지 진입 시 → 4개의 엔드포인트를 순차적으로 시도하여 루트 목록 로드
- 같은 게시글 재방문 → 캐시 없이 완전히 새로운 요청 발생

#### (3) Google Maps API 즉시 로드

지도 페이지를 방문하지 않아도 Google Maps JavaScript API가 앱 초기화 시점에 로드될 가능성이 높다. 지연 로드(lazy loading) 전략이 보이지 않으며, 마커 클러스터링도 적용되지 않아 핀이 많아질 경우 렌더링 성능이 급격히 저하된다.

#### (4) Zustand 스토어의 불필요한 리렌더링

`App.jsx`에서 inline selector 함수를 사용하여 매 렌더링마다 새로운 구독이 생성된다. `useMapStore`의 `toggleMediaType`은 변경이 없어도 매번 새 배열 참조를 생성하여 지도 필터 토글 시 전체 지도가 리렌더링된다. `PostCard`, `ArtworkCard` 등의 리스트 아이템에 `React.memo`가 적용되지 않아 부모 리스트 변경 시 전체 아이템이 다시 렌더링된다.

### 2.2 백엔드 측 로딩 지연 요인

#### (1) 페이지네이션 미구현 — 전체 데이터 반환

`GET /api/v1/posts`, `GET /api/v1/posts/community`, `GET /api/v1/main/` 등 모든 목록 조회 API가 전체 데이터를 배열로 반환한다. Spring Data의 Pageable이 적용되지 않았다. 데이터가 수천 건으로 증가하면 응답 시간이 급격히 증가하고, 네트워크 전송량이 급증한다.

#### (2) N+1 쿼리 문제

`PostResponse.fromEntity()`에서 게시글 1건 조회 시 연관된 이미지, 루트, 루트스팟을 각각 별도 쿼리로 가져온다. 게시글 10건 목록을 조회하면 **최소 30건 이상의 쿼리가 발생**한다. `@EntityGraph`나 JOIN FETCH가 적용되지 않았다.

#### (3) 메모리 기반 검색 필터링

`PostServiceImpl.getAllPost()`에서 **전체 게시글을 DB에서 가져온 뒤**, Java Stream으로 제목/내용 문자열 검색을 수행한다. 데이터베이스 레벨의 LIKE 쿼리나 전문 검색 인덱스가 아닌, 애플리케이션 메모리에서 필터링하므로 데이터 증가에 따라 심각한 성능 저하가 발생한다.

#### (4) 조회수 갱신의 쓰기 잠금

게시글 조회(GET) 시 `entity.incrementViewCount()`로 **매 읽기 요청마다 UPDATE 쿼리가 실행**된다. 이로 인해 read-heavy 시나리오에서 데이터베이스 쓰기 잠금 경합이 발생하며, 동시 접속자가 늘어날수록 응답 지연이 심화된다.

#### (5) 인덱스 부재

엔티티 어노테이션에 `@Index`가 정의되어 있지 않다. `Post.userId`, `Post.routeId`, `Post.createdAt`, `Comment.postId` 등 빈번하게 조회되는 컬럼에 인덱스가 없어 테이블 풀 스캔이 발생할 수 있다.

---

## 3. 프론트엔드 코드의 구조적 문제 (무법칙·무질서 분석)

### 3.1 설계 문서와 실제 구현의 괴리

프로젝트 제안서에서는 다음과 같은 구조화 전략을 명시했다:

- **Atomic Design 패턴** (atoms → molecules → organisms)
- **커스텀 훅을 통한 관심사 분리**
- **React Context API 기반 전역 상태 관리**

실제 코드에서는 이 중 **어느 것도 제대로 이행되지 않았다**.

- Atomic Design → 실제로는 **feature별 폴더 구조** (post/, map/, auth/)
- 커스텀 훅 → 일부 페이지(PostDetailPage)에만 적용, 나머지는 인라인 로직
- Context API → **전혀 사용되지 않음**, 대신 Zustand 2개 스토어만 사용

### 3.2 단일 책임 원칙(SRP) 위반

`routePostApi.js` (965줄)가 가장 심각한 사례다. 이 파일 하나가 다음 역할을 모두 수행한다:

- 응답 정규화 (200줄)
- 루트 옵션 빌딩 (100줄)
- CRUD 연산 (200줄)
- 댓글 처리 (50줄)
- 북마크 처리 (100줄)
- 이벤트 발행
- JWT 토큰 파싱

이 파일은 최소 6개 이상의 모듈로 분리되어야 한다.

### 3.3 DRY 원칙 위반 — JWT 토큰 파싱 3중 복제

JWT 토큰에서 userId를 추출하는 로직이 3곳에서 독립적으로 구현되어 있다:

1. `useAuthStore.js` (22~36행) — parseTokenPayload
2. `routePostApi.js` (52~71행) — resolveUserIdForRouteQuery
3. `communityApi.js` (42~62행) — resolveUserId

한 곳에서 버그가 수정되어도 나머지 2곳은 그대로 남으며, 보안 취약점이 발견될 경우 3곳 모두를 수정해야 한다.

### 3.4 에러 처리 전략 부재

- **Error Boundary 미구현**: 컴포넌트 크래시 시 전체 앱이 백지 화면으로 전환된다.
- **로딩 UI 미비**: 모든 로딩 상태가 `<div>로딩 중...</div>` 같은 단순 텍스트로만 처리된다. 스켈레톤 UI, 스피너, 재시도 버튼이 없다.
- **에러 복구 불가**: API 실패 시 사용자에게 재시도 방법이 제공되지 않는다.
- **일관성 없는 에러 핸들링**: 일부는 try-catch, 일부는 `.catch(() => [])`, 일부는 아예 에러 처리 없음.
- **API 타임아웃 미설정**: native fetch에 타임아웃이 없어 네트워크 문제 시 무한 대기 상태에 빠질 수 있다.

### 3.5 CSS/스타일링의 무질서

35개 이상의 CSS Module 파일이 디자인 시스템 없이 독립적으로 작성되었다:

- CSS 변수(custom properties)가 정의되어 있지 않아 색상, 간격, 타이포그래피가 파일마다 다르다.
- 동일한 역할의 간격이 `gap: 12px`, `gap: 1rem`, `gap: 15px` 등으로 중구난방이다.
- 공유 유틸리티 클래스가 없어 카드, 버튼, 모달, 배지의 스타일이 각 컴포넌트에서 개별적으로 정의된다.
- 다크모드 색상이 `index.css`에 하드코딩되어 있으나 재사용되지 않는다.

### 3.6 하드코딩된 설정값

- **API 기본 URL**: `FetchClient.js`에 `http://localhost:9933`이 직접 작성되어 있다. 환경 변수로 관리되지 않으므로 프로덕션 배포 시 반드시 실패한다.
- **TMDB 엔드포인트**: 외부 API URL이 비즈니스 로직 파일 내부에 직접 작성되어 있다.
- **매직 넘버**: 좌표값, 제한값, 장르 ID 등이 상수 정의 없이 코드 곳곳에 산재한다.

### 3.7 타입 안전성 완전 부재

150개 이상의 소스 파일이 모두 JavaScript로 작성되었으며 TypeScript가 전혀 사용되지 않았다. PropTypes조차 정의되어 있지 않아 컴파일 타임 에러 검출이 불가능하고, API 응답 형태가 어디에도 명시되지 않아 런타임 오류 가능성이 높다.

---

## 4. 백엔드 주요 문제점

### 4.1 보안 취약점 (CRITICAL)

#### 소스 코드 내 인증 정보 하드코딩

`application.properties`에 다음 정보가 평문으로 노출되어 있다:

- MySQL 사용자명/비밀번호
- TMDB API 키
- Gmail SMTP 비밀번호

`JwtProvider.java`에 JWT 시크릿 키가 소스 코드에 직접 작성되어 있다. 이 코드가 Git 저장소에 커밋된 상태이므로, 저장소 접근 권한이 있는 누구든 전체 데이터베이스에 접근하고 JWT 토큰을 위조할 수 있다.

#### 인가(Authorization) 결함

`PostController.deletePost()`에서 `userId`를 요청 파라미터로 받고 있다. SecurityContext에서 현재 인증된 사용자 ID를 추출하는 것이 아니라 클라이언트가 보낸 값을 신뢰하므로, 다른 사용자의 게시글을 삭제할 수 있는 **권한 우회 취약점**이 존재한다.

#### CORS 설정 미흡

`http://localhost:5173` 단일 오리진만 허용하고, 모든 헤더를 와일드카드로 허용한다. 프로덕션 배포 시 CORS가 차단되고, 와일드카드 헤더는 보안 위험 요소다.

### 4.2 API 설계 비일관성

- URL 패턴: 단수/복수 혼재 (`/spots` vs `/maps`), 중첩/평면 혼재
- HTTP 상태 코드: 게시글 생성이 201이 아닌 200을 반환
- DTO 명명: `PostRequest` / `PostCreateRequest` / `PostEntryResponse` 등 역할 구분이 불명확
- 11개의 중복 Repository 메서드: 정렬 기준만 다른 `findAllBy*` 메서드가 통합되지 않음

### 4.3 예외 처리 비일관성

- `BusinessException`, `EntityNotFoundException`, `IllegalArgumentException`이 혼용된다.
- 동일한 "게시글 없음" 상황에서 서로 다른 예외 타입이 발생한다.
- DTO 입력 유효성 검증(`@Valid`, `@NotBlank`, `@Size`)이 대부분 적용되지 않았다.

---

## 5. 일일회의 기록에서 드러나는 프로세스 문제

WBS 일일회의 문서(62건)를 분석한 결과 다음 패턴이 확인된다:

### 5.1 반복되는 차단 요소

- **CSS 충돌**: 3/14, 3/15, 3/16에 걸쳐 반복적으로 보고되며 근본 해결 없이 임시 수정만 반복
- **프론트-백 연동 불일치**: API 반환값 형식에 대한 합의 부재로 조경철이 3/11, 3/12에 "백에서 어떤 형태로 넘어올지 파악 필요"라고 기록
- **DB 마이그레이션 의존성**: 김지우가 3/12에 "석현이가 마이그레이션 새 테이블 적용해주면 이메일 인증 가능"이라고 대기 상태 보고

### 5.2 프론트엔드 담당자 과중

조경철이 프론트엔드 UI/UX 구현의 거의 대부분을 담당하고 있으며, 메인페이지, 마이페이지, 업적 페이지, PostCard, 게시글 목록/상세/작성/수정/삭제, 댓글 UI, 맵/스팟/루트 UI를 혼자 진행하고 있다. 코드 리뷰 없이 개별 작업 후 PR만 올리는 패턴이 반복되어 코드 일관성이 유지되지 않았다.

### 5.3 API 명세 지연

- 3/10: "API 명세서 경로 형식 통일 필요" (안성민)
- 3/12: "PostPage api return값 정리 회의 필요" (조경철)
- 프론트-백 간 API 계약(contract)이 사전에 확정되지 않아 각자 개발 후 연동 시 불일치가 발생

---

## 6. 요구사항 대비 구현 격차

요구사항 정의서(32건) 대비 미구현 또는 불완전한 항목:

| 요구사항 ID | 기능 | 상태 |
|---|---|---|
| B_003 | 북마크 커스텀 이름 설정 | 미확인 |
| B_005 | 북마크 폴더 관리 | 미확인 |
| M_005 | 단축 URL 공유 | 미구현 |
| P_004 | 게시글 태그설정 (금칙어 검사) | 금칙어 검사 미구현 |
| P_008 | 콘텐츠 신고 | API 존재하나 완성도 불확실 |
| S_001 | 서비스 응답 속도 (3초 이내) | 페이지네이션 미구현으로 미달 가능성 |
| U_004 | 비밀번호 찾기 | Rate limiting 없음, 보안 미흡 |

---

## 7. 개선 방향 제안

### 즉시 조치 (Critical)

1. **보안 인증 정보 외부화**: application.properties의 모든 비밀번호, API 키를 환경 변수로 이전. JWT 시크릿 키도 소스에서 제거.
2. **인가 로직 수정**: 요청 파라미터가 아닌 SecurityContext에서 userId를 추출하도록 모든 컨트롤러 수정.
3. **Error Boundary 추가**: React 앱 최상위에 Error Boundary를 배치하여 컴포넌트 크래시 시 앱 전체 중단 방지.

### 단기 조치 (High Priority)

4. **코드 스플리팅 적용**: `React.lazy()` + `Suspense`로 페이지별 지연 로딩 적용. 초기 번들 크기를 50% 이상 줄일 수 있다.
5. **페이지네이션 구현**: 모든 목록 API에 Spring Data Pageable 적용. 기본 페이지 크기 20건.
6. **N+1 쿼리 해결**: `@EntityGraph` 또는 `JOIN FETCH`를 적용하여 게시글 목록 조회 쿼리를 1~3건으로 축소.
7. **JWT 토큰 파싱 중앙화**: `utils/tokenUtils.js` 하나의 모듈로 통합하고 3곳의 중복 코드 제거.
8. **routePostApi.js 분리**: normalize.js, crud.js, comments.js, bookmarks.js 등 6개 이상의 모듈로 분할.

### 중기 조치 (Medium Priority)

9. **데이터 페칭 라이브러리 도입**: React Query 또는 SWR을 도입하여 자동 캐싱, 중복 요청 제거, 배경 갱신(stale-while-revalidate) 적용.
10. **디자인 시스템 구축**: CSS 변수로 색상, 간격, 타이포그래피 토큰을 정의하고 모든 컴포넌트에서 참조.
11. **API 명세 사전 확정**: OpenAPI/Swagger 기반으로 프론트-백 API 계약을 먼저 확정하고 개발 진행.
12. **검색 로직 DB 이관**: 메모리 기반 문자열 필터링을 DB 쿼리(LIKE 또는 Full-Text Index)로 전환.
13. **데이터베이스 인덱스 추가**: 자주 조회되는 FK 및 정렬 컬럼에 인덱스 정의.
14. **로딩/에러 UI 컴포넌트화**: 스켈레톤 로더, 에러 표시, 재시도 버튼을 공통 컴포넌트로 제작.

### 장기 조치 (Nice to Have)

15. **TypeScript 점진적 도입**: 새 파일부터 .tsx로 작성하고, API 응답 타입을 먼저 정의.
16. **테스트 코드 작성**: 유틸리티 함수와 스토어에 대한 단위 테스트 우선 도입.
17. **조회수 비동기 처리**: 조회수 증가를 `@Async` 또는 Redis 기반 배치 처리로 전환하여 읽기 성능 개선.
18. **캐싱 전략 수립**: 작품 목록, 태그 목록 등 정적 데이터에 Redis 캐시 적용.

---

## 8. 결론

推し足 프로젝트는 도메인 모델 설계(ERD, DB 설계)와 백엔드 아키텍처의 기본 골격(도메인 주도 모듈 분리, Controller-Service-Repository 패턴)은 적절하게 수립되었다. 그러나 실제 구현 과정에서 설계 문서의 원칙이 이행되지 않았고, 특히 프론트엔드에서 체계적 아키텍처 없이 기능 구현에만 집중한 결과 코드의 일관성, 성능, 유지보수성에서 심각한 문제가 발생했다.

로딩 문제의 핵심은 **프론트엔드의 코드 스플리팅 부재 + API 캐싱 부재**와 **백엔드의 페이지네이션 미구현 + N+1 쿼리**의 복합 작용이다. 이 4가지 문제를 우선 해결하면 체감 성능이 크게 개선될 것으로 예상된다.

보안 측면에서는 소스 코드 내 인증 정보 하드코딩과 요청 파라미터 기반 인가 로직이 가장 시급한 수정 대상이다. 이 두 가지는 서비스 오픈 전 반드시 해결해야 한다.
